<div class="row">
  <div class="py-5 col-lg-12">
    <div class="card">

      <div class="card-header">
        <div class="row">
          <h1 class="col-lg-12">
            <%= title 'Top Viewpoints' %>
          </h1>

        </div>
      </div>

      <div class="card-body">

        <%= render partial: 'photos_display', locals: { photos: @photos } %>

        <br/>
        <%= paginate @photos %>
      </div>

    </div>
  </div>
</div>